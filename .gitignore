# Original uncompressed images kept locally as a backup (do not commit — ~14 MB)
assets/_originals/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.vscode/
.idea/

# Stray uploads / scratch
uploads/

# Node (just in case)
node_modules/

# Secrets — real mail credentials live only on the server, never in Git
config.php
phpmailer/

# Claude Code local state
.claude/
